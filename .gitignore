# 🚀 Astro.js .gitignore

# Node.js
node_modules/
pnpm-lock.yaml
yarn.lock
package-lock.json

# Directorios de compilación y caché
dist/          # ⚠️ Astro genera 'dist/' en cada build, no lo subas
.out/          # Caché de Astro
.cache/        # Caché de compilaciones
.vite/         # Caché de Vite
*.log          # Archivos de log (npm-debug.log, etc.)

# Archivos de configuración del entorno
.env
.env.local
.env.*.local   # Evita subir variables de entorno privadas

# Archivos del editor
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace

# GitHub & OS Files
.DS_Store      # macOS metadata
Thumbs.db      # Windows metadata

# Astro Deploy (Si usas GitHub Pages con Actions, ignora estas)
.github/